# ================================
# 环境配置示例文件
# ================================
# 复制此文件为 .env 并修改配置值
# cp .env.example .env

# ================================
# Flask 应用配置
# ================================
# Flask 环境：development（开发）或 production（生产）
FLASK_ENV=development

# 应用配置名称：development 或 production（可选，优先于 FLASK_ENV）
APP_CONFIG=development

# Flask 密钥（用于会话加密，请使用随机字符串）
# 生成方式：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_ME_USE_A_LONG_RANDOM_SECRET

# Flask 调试模式：True 或 False
DEBUG=True

# 是否开启调度器管理 API（建议默认关闭，按需开启）
ENABLE_SCHEDULER_API=false

# 是否开启测试登录 API（仅开发调试使用）
ENABLE_TEST_LOGIN_API=false

# ================================
# 数据库配置
# ================================
# MySQL 数据库主机地址
DB_HOST=localhost

# MySQL 数据库端口
DB_PORT=3306

# MySQL 数据库用户名
DB_USER=root

# MySQL 数据库密码
DB_PASSWORD=CHANGE_ME_DB_PASSWORD

# 数据库名称
DB_NAME=movies_recommend

# ================================
# JWT 令牌配置（用于 API 认证）
# ================================
# JWT 密钥（请使用与 SECRET_KEY 不同的随机字符串）
JWT_SECRET_KEY=CHANGE_ME_USE_A_DIFFERENT_LONG_RANDOM_SECRET

# JWT 访问令牌过期时间（小时）
JWT_ACCESS_TOKEN_EXPIRES=24

# JWT 刷新令牌过期时间（天）
JWT_REFRESH_TOKEN_EXPIRES=30

# ================================
# 管理员配置
# ================================
# 管理员注册验证码（注册管理员账号时需要）
ADMIN_VERIFICATION_CODE=CHANGE_ME_ADMIN_CODE

# 默认密码（可选，用于重置密码）
DEFAULT_PASSWORD=CHANGE_ME_STRONG_DEFAULT_PASSWORD

# 密码最小长度（登录/注册/改密统一策略）
PASSWORD_MIN_LENGTH=8

# ================================
# 应用配置
# ================================
# 应用运行主机
HOST=0.0.0.0

# 应用运行端口
PORT=5000

# 每页显示的电影数量
PER_PAGE=12

# ================================
# TMDB API 配置（可选）
# ================================
# 如果需要爬取 TMDB 数据，请配置以下信息
# TMDB API Key（从 https://www.themoviedb.org/settings/api 获取）
# TMDB_API_KEY=your-tmdb-api-key

# TMDB API 基础 URL
# TMDB_BASE_URL=https://api.themoviedb.org/3

# ================================
# 日志配置
# ================================
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径
LOG_FILE=logs/app.log

# ================================
# 缓存配置（可选，用于 Flask-Caching）
# ================================
# 缓存类型：simple, redis, memcached
# CACHE_TYPE=simple

# Redis 配置（如果使用 redis 缓存）
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=

# ================================
# 邮件配置（可选，用于密码重置等功能）
# ================================
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@example.com
# MAIL_PASSWORD=your-email-password
# MAIL_DEFAULT_SENDER=your-email@example.com

# ================================
# 其他配置
# ================================
# 会话持续时间（天）
PERMANENT_SESSION_LIFETIME=7

# 上传文件大小限制（MB）
MAX_CONTENT_LENGTH=16

# 允许的文件扩展名（逗号分隔）
ALLOWED_EXTENSIONS=png,jpg,jpeg,gif

# ================================
# 安全提示
# ================================
# 1. 不要将 .env 文件提交到 Git 仓库
# 2. 生产环境务必修改所有密钥和密码
# 3. 定期更换密钥以提高安全性
# 4. 使用强密码和复杂的密钥
